export type RegisterDTO = {
    fullName: string;
    email: string;
    passwordUsers: string;
    username: string;
    imgage?: string;
};

export type LoginDTO = {
    email: string;
    passwordUsers: string;
  };

